# AGENTS.md

## سياق المشروع وتوجيهات التطوير

هذا المستودع يمثل نظاماً متكاملاً لإدارة الموارد البشرية والرواتب للموظفين في العراق وفق سلم رواتب 2023 الموحد.

## البنية الأساسية والمصادقة

- **المصادقة (Authentication):** النظام يعتمد بالكامل على نظام مصادقة مستقل باستخدام **JWT + bcrypt + PostgreSQL** عبر وسيط الحماية الخلفي `src/middleware/auth.ts` ومسار تسجيل الدخول `POST /api/auth/login` في `server.ts` (ولا يستخدم Firebase).
- **منطق حساب الرواتب (Salary Logic):** يتم حساب الراتب الاسمي والبدلات والاستقطاعات في `src/lib/salaryTable.js` عبر دالة `calculateSalary` وفق نظام هجين (Hybrid):
  - قيم مرجعية افتراضية لسلم رواتب 2023 الموحد والدرجات والمراحل في الكود.
  - إمكانية التعديل والتجاوز (Override) من جدول `salaryScale` في قاعدة البيانات من خلال لوحة التحكم كأولوية قصوى.

## الملفات الرئيسية ومواقعها

- `src/`: كود الواجهة الأمامية (Frontend) الخاص بنظام الموارد البشرية (React + Vite + TailwindCSS).
- `src/api/apiClient.js`: عميل الاتصال الخلفي الموحد وإدارة توكنات الجلسة (`hr_session_token`).
- `src/middleware/auth.ts`: وسيط التحقق من صحة توكنات JWT المشفرة وحماية المسارات.
- `src/lib/salaryTable.js`: محرك حساب الرواتب والمخصصات والاستقطاعات وسنوات الترفيع والعلاوات السنوية.
- `server.ts`: الخادم الخلفي (Express + TypeScript) للتعامل مع قاعدة بيانات PostgreSQL وإدارة المسارات.
- `src/db/`: مجلد المخططات والعلاقات وقاعدة البيانات (Drizzle ORM).
- `.env`: متغيرات البيئة الخاصة بقاعدة البيانات والمصادقة (غير متتبع في Git).
- `.env.example`: قالب متغيرات البيئة الآمن للمشروع.

## بيئة العمل المحلية

- يتم تشغيل الواجهة الأمامية والواجهة الخلفية معاً باستخدام الأمر التالي:
  ```bash
  npm run dev
  ```
- يتم تشغيل الاختبارات الآلية باستخدام:
  ```bash
  npm test
  ```
- يتم بناء الخادم والواجهة الأمامية للإنتاج باستخدام:
  ```bash
  npm run build
  ```
- يتم تشغيل الخادم المبني للإنتاج باستخدام:
  ```bash
  npm run start
  ```
